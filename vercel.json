{
  "buildCommand": "cd arroz-frontend && npm install && npm run build",
  "outputDirectory": "arroz-frontend/dist",
  "installCommand": "cd arroz-frontend && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}